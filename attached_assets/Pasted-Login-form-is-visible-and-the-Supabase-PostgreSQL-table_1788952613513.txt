Login form is visible and the Supabase PostgreSQL tables were created successfully, but when I enter the Super Admin username/password and click "Masuk ke dashboard", nothing happens.

Debug this carefully without rewriting the whole app.

Important:
- Do not change the database provider.
- Do not switch to Supabase Auth.
- Do not change from username/password login.
- Do not print or expose any secret values.
- Do not delete existing schema or data.
- Do not implement Phase 2 features.

Please inspect and fix the login flow.

Check these areas:

1. Frontend form submit
- Verify the login form has a proper submit handler.
- Verify the button is type="submit".
- Verify loading and error states are visible.
- If login fails, show a clear Indonesian error message on the page.
- If login succeeds, redirect to /dashboard.

2. Login API / server action
- Verify whether the login request is actually called.
- Check the login route/action receives username and password.
- Check zod validation does not silently fail.
- Check the route returns clear JSON/error or redirects properly.
- Add useful server logs that do not expose passwords or secrets.

3. Super Admin account
- Verify SUPER_ADMIN_USERNAME exists in env without printing its value.
- Verify a user with that username exists in the User table.
- Verify the stored passwordHash is not empty.
- Verify bcrypt password comparison works.
- If the Super Admin seed is broken, fix the seed script and rerun it safely using upsert.

4. Session cookie
- Verify AUTH_SECRET exists without printing it.
- Verify the session token is created after successful login.
- Verify the cookie is set as HTTP-only.
- In development/Replit, do not set secure=true unless the environment supports it correctly.
- Use secure cookie only in production.
- Verify cookie path is "/".
- Verify /dashboard can read the cookie.

5. Redirect behavior
- Verify router.push("/dashboard") or redirect("/dashboard") is actually executed after successful login.
- Verify middleware is not redirecting back to /login because it cannot read the session.
- Verify dashboard route protection uses the same cookie name and same auth utility as login.

6. Manual verification
After fixing:
- Run npm test.
- Run npx tsc --noEmit.
- Run npm run build.
- Restart the dev server if needed.
- Test login through the preview.
- Report exactly what was broken and what was changed.

Expected behavior:
- Wrong credentials show Indonesian error.
- Correct Super Admin credentials redirect to /dashboard.
- Dashboard shows authenticated Super Admin information.